🧠 What you just learned (important)

State lives at page level

Reducer handles data only

UI comes after structure

You resisted the urge to overcode (most devs fail here)

=====================================================================
┌─────────────────────────────────────────────────────────────────────┐
│                           DELETE IMAGE FLOW                        │
└─────────────────────────────────────────────────────────────────────┘

🖱️  STEP 1: USER CLICKS DELETE BUTTON
┌─────────────────────────────────────────────────────────────────────┐
│  User clicks "Delete" on image card                                │
│  Image ID: "gallery/anime/1705234567890"                           │
└─────────────────────────────────────────────────────────────────────┘
                                    ↓

🎯 STEP 2: GALLERYCARD CALLS CALLBACK
┌─────────────────────────────────────────────────────────────────────┐
│  GalleryCard.jsx:                                                  │
│  <button onClick={() => onDelete(image.id)}>Delete</button>        │
│                                                                     │
│  Calls: onDelete("gallery/anime/1705234567890")                    │
└─────────────────────────────────────────────────────────────────────┘
                                    ↓

🧠 STEP 3: GALLERYPAGE HANDLES DELETE
┌─────────────────────────────────────────────────────────────────────┐
│  GalleryPage.jsx:                                                  │
│  const handleDelete = async (id) => {                              │
│    try {                                                           │
│      await deleteImage(id);  ← Calls API function                 │
│      dispatch({ type: ACTIONS.DELETE, payload: id });             │
│    } catch (err) {                                                 │
│      console.error('Failed to delete', err);                      │
│    }                                                               │
│  }                                                                 │
└─────────────────────────────────────────────────────────────────────┘
                                    ↓

🌐 STEP 4: API FUNCTION PROCESSES REQUEST
┌─────────────────────────────────────────────────────────────────────┐
│  galleryApi.js:                                                    │
│  export const deleteImage = async (publicId) => {                  │
│    const encodedId = encodeURIComponent(publicId);                 │
│    // "gallery/anime/1705234567890" → "gallery%2Fanime%2F1705..."  │
│                                                                     │
│    const res = await fetch(`${API_URL}/${encodedId}`, {            │
│      method: 'DELETE',                                             │
│    });                                                             │
│    // URL: http://localhost:5000/api/gallery/gallery%2F...         │
│  }                                                                 │
└─────────────────────────────────────────────────────────────────────┘
                                    ↓

🚀 STEP 5: HTTP REQUEST SENT TO BACKEND
┌─────────────────────────────────────────────────────────────────────┐
│  HTTP Request:                                                      │
│  DELETE http://localhost:5000/api/gallery/gallery%2Fanime%2F1705... │
│                                                                     │
│  Browser → Network → Backend Server                                │
└─────────────────────────────────────────────────────────────────────┘
                                    ↓

🛣️  STEP 6: EXPRESS ROUTING
┌─────────────────────────────────────────────────────────────────────┐
│  gallery.routes.js:                                                │
│  router.delete("/:public_id", deleteImage);                        │
│                                                                     │
│  Express matches: DELETE /:public_id                               │
│  Calls: deleteImage controller function                            │
└─────────────────────────────────────────────────────────────────────┘
                                    ↓

🎛️  STEP 7: BACKEND CONTROLLER PROCESSES
┌─────────────────────────────────────────────────────────────────────┐
│  gallery.controller.js:                                            │
│  export const deleteImage = async (req, res) => {                  │
│    const publicId = decodeURIComponent(req.params.public_id);      │
│    // "gallery%2Fanime%2F1705..." → "gallery/anime/1705..."        │
│                                                                     │
│    await cloudinary.uploader.destroy(publicId);                    │
│    res.json({ success: true });                                    │
│  }                                                                 │
└─────────────────────────────────────────────────────────────────────┘
                                    ↓

☁️  STEP 8: CLOUDINARY DELETES IMAGE
┌─────────────────────────────────────────────────────────────────────┐
│  Cloudinary Service:                                               │
│  - Receives: "gallery/anime/1705234567890"                         │
│  - Finds image in cloud storage                                    │
│  - Permanently deletes the image file                              │
│  - Returns: Success confirmation                                   │
└─────────────────────────────────────────────────────────────────────┘
                                    ↓

📤 STEP 9: BACKEND SENDS RESPONSE
┌─────────────────────────────────────────────────────────────────────┐
│  HTTP Response:                                                     │
│  Status: 200 OK                                                    │
│  Body: { "success": true }                                         │
│                                                                     │
│  Backend → Network → Frontend                                      │
└─────────────────────────────────────────────────────────────────────┘
                                    ↓

🔄 STEP 10: API FUNCTION RETURNS DATA
┌─────────────────────────────────────────────────────────────────────┐
│  galleryApi.js:                                                    │
│  if (!res.ok) throw new Error('Failed to delete image');           │
│  return res.json();  // Returns: { success: true }                 │
│                                                                     │
│  deleteImage() resolves with: { success: true }                    │
└─────────────────────────────────────────────────────────────────────┘
                                    ↓

🎯 STEP 11: GALLERYPAGE UPDATES STATE
┌─────────────────────────────────────────────────────────────────────┐
│  GalleryPage.jsx:                                                  │
│  const handleDelete = async (id) => {                              │
│    await deleteImage(id);  // ✅ Success!                          │
│    dispatch({ type: ACTIONS.DELETE, payload: id });  ← Runs this  │
│  }                                                                 │
└─────────────────────────────────────────────────────────────────────┘
                                    ↓

🧮 STEP 12: REDUCER UPDATES STATE
┌─────────────────────────────────────────────────────────────────────┐
│  galleryReducer.jsx:                                               │
│  case ACTIONS.DELETE:                                              │
│    return {                                                        │
│      ...state,                                                     │
│      images: state.images.filter(                                  │
│        img => img.id !== action.payload  // Remove deleted image   │
│      ),                                                            │
│    }                                                               │
└─────────────────────────────────────────────────────────────────────┘
                                    ↓

🎨 STEP 13: UI RE-RENDERS
┌─────────────────────────────────────────────────────────────────────┐
│  React Re-render:                                                  │
│  - State changed → Component re-renders                            │
│  - GalleryGrid receives new images array (without deleted image)   │
│  - GalleryCard for deleted image disappears from screen            │
│  - User sees image is gone ✅                                      │
└─────────────────────────────────────────────────────────────────────┘

✅ DELETE COMPLETE!
